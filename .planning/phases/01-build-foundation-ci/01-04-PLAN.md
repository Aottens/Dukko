---
phase: 01-build-foundation-ci
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - README.md
  - .github/workflows/build.yml  # conditional — only if Task 1 TBD #6 reveals build artifact path mismatch
  - source/PluginProcessor.cpp   # conditional — only if Task 1 pluginval strict-10 fails on state round-trip (Pitfall 3 fix)
autonomous: false
requirements: [BUILD-02]
must_haves:
  truths:
    - "A local Release build produces both Dukko.vst3 and Dukko.clap, and `lipo -archs` reports `arm64` for both binaries (BUILD-02)."
    - "COPY_PLUGIN_AFTER_BUILD has installed both bundles into ~/Library/Audio/Plug-Ins/VST3/ and ~/Library/Audio/Plug-Ins/CLAP/ on the developer machine (D-09)."
    - "The Dukko GitHub repo exists, is public (D-08), the working tree's git remote points at it, the initial commit has been pushed, and the macos-14 Actions job has gone green on first run (BUILD-04 + QUAL-01 first-push verification)."
    - "The user has manually loaded both VST3 and CLAP variants of Dukko in Bitwig under vendor 'Dukko Audio' and confirmed the plugin instantiates without error (manual-only verification per VALIDATION.md)."
    - "README.md badge URL has been updated from <your-org> to the actual GitHub username/org and the badge renders green on the GitHub repo landing page."
  artifacts:
    - path: "README.md"
      provides: "Final README with the actual GitHub org substituted into the CI badge URL"
      contains: "actions/workflows/build.yml/badge.svg"
  key_links:
    - from: "Local build output"
      to: "~/Library/Audio/Plug-Ins/VST3/Dukko.vst3 + ~/Library/Audio/Plug-Ins/CLAP/Dukko.clap"
      via: "JUCE COPY_PLUGIN_AFTER_BUILD post-build install (D-09)"
      pattern: "COPY_PLUGIN_AFTER_BUILD"
    - from: "Local working tree"
      to: "github.com/<your-org>/Dukko"
      via: "git push origin main → triggers Actions workflow"
      pattern: "git push"
    - from: ".github/workflows/build.yml"
      to: "Bitwig (manual host load)"
      via: "Developer drag-loads the installed bundles into Bitwig"
      pattern: "Activity Monitor confirms Bitwig is Apple (arm64), not Intel"
---

<objective>
Close out Phase 1 by running the first end-to-end verification: local build produces both bundles in arm64, GitHub repo is created and the first push triggers a green CI run, and the developer manually loads both VST3 + CLAP variants in Bitwig to confirm host acceptance. This plan owns BUILD-02 (the "loads in Bitwig as native arm64" requirement, which is fundamentally a manual verification) plus the first-push portions of BUILD-04 and QUAL-01.

Purpose: All previous plans in Phase 1 (01, 02, 03) define the artifacts; this plan EXERCISES them and proves Phase 1's success criteria are TRUE end-to-end. Until this plan runs, the phase is not actually verified — only built.

Output: Updated README.md badge URL, a green Actions badge on the GitHub repo landing page, working VST3 + CLAP in Bitwig, and a Phase 1 SUMMARY rolling up the four plans for STATE.md update.

This plan is `autonomous: false` because:
1. Manual-only Bitwig load test (per VALIDATION.md "Manual-Only Verifications") cannot be scripted.
2. GitHub repo creation requires either `gh repo create` invocation that the user authenticates, or manual web UI flow — both surface as a checkpoint.
3. Substituting `<your-org>` in the README badge requires knowing the user's GitHub org/username, which is a checkpoint:decision (or human-action) input.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-build-foundation-ci/01-CONTEXT.md
@.planning/phases/01-build-foundation-ci/01-RESEARCH.md
@.planning/phases/01-build-foundation-ci/01-VALIDATION.md
@.planning/phases/01-build-foundation-ci/01-01-SUMMARY.md
@.planning/phases/01-build-foundation-ci/01-02-SUMMARY.md
@.planning/phases/01-build-foundation-ci/01-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local build + arm64 verification + COPY_PLUGIN_AFTER_BUILD confirmation</name>
  <files>
    - (primary: verification + diagnostic only — no files modified by default)
    - .github/workflows/build.yml — CONDITIONAL: only edited if step 3 reveals build artifact paths differ from what plan 03 hardcoded (TBD #6 resolution)
    - source/PluginProcessor.cpp (or actual processor filename after the rename) — CONDITIONAL: only edited if step 6 pluginval strict-10 fails on the unmodified Pamplejuce shell with a state round-trip error (Pitfall 3 in-Phase-1 fix)
  </files>
  <read_first>
    - .planning/phases/01-build-foundation-ci/01-VALIDATION.md §"Per-Task Verification Map" (BUILD-01, BUILD-02, BUILD-03 commands)
    - .planning/phases/01-build-foundation-ci/01-RESEARCH.md §"Pitfalls 4 + 5" (Gatekeeper ad-hoc signing on Apple Silicon; CLAP artifact path)
    - .planning/phases/01-build-foundation-ci/01-RESEARCH.md §"Open TBDs" #4, #5, #6 (verify COPY copies CLAP, codesign, actual paths)
    - .planning/phases/01-build-foundation-ci/01-CONTEXT.md (D-09: COPY_PLUGIN_AFTER_BUILD=TRUE)
  </read_first>
  <action>
    From the working tree root, run a clean Release build and confirm both bundles materialise as arm64.

    1. Configure + build:
       ```bash
       rm -rf build
       cmake -B build -DCMAKE_BUILD_TYPE=Release
       cmake --build build --config Release --parallel
       ```
       The first run is slow (CPM downloads JUCE 8.0.12 + clap-juce-extensions @ SHA + chowdsp_utils v2.4.0). Subsequent runs use the cache.

    2. Confirm both bundles exist:
       ```bash
       find build -name 'Dukko.vst3' -o -name 'Dukko.clap'
       ```
       Expect at least two paths. Typical layout per RESEARCH.md Pitfall 5:
       - `build/Dukko_artefacts/Release/VST3/Dukko.vst3`
       - `build/Dukko_artefacts/Release/CLAP/Dukko.clap`
       If paths differ, record them — they may need to be reflected back into `.github/workflows/build.yml` (plan 03 used the conventional Pamplejuce paths; if they are different here, edit the workflow before pushing).

    3. **Resolve TBD #6 (CLAP/VST3 output paths).** Compare actual `find` output against the workflow's hardcoded paths. If they differ, edit `.github/workflows/build.yml` to match — then re-run the verify command in plan 03's task 1 to confirm the workflow YAML is still valid.

    4. Verify arm64 architecture for both:
       ```bash
       lipo -archs build/Dukko_artefacts/Release/VST3/Dukko.vst3/Contents/MacOS/Dukko | grep -w arm64
       lipo -archs build/Dukko_artefacts/Release/CLAP/Dukko.clap/Contents/MacOS/Dukko | grep -w arm64
       ```
       Both must exit 0 and emit `arm64` (BUILD-02).

    5. **Resolve TBD #4 + #5 (COPY_PLUGIN_AFTER_BUILD covers CLAP; ad-hoc codesign).** Confirm both bundles were installed to `~/Library/Audio/Plug-Ins/`:
       ```bash
       ls -la ~/Library/Audio/Plug-Ins/VST3/Dukko.vst3
       ls -la ~/Library/Audio/Plug-Ins/CLAP/Dukko.clap
       ```
       If the CLAP bundle is missing from `~/Library/Audio/Plug-Ins/CLAP/`, COPY_PLUGIN_AFTER_BUILD only handled VST3. Add a CMake `add_custom_command(TARGET Dukko POST_BUILD ...)` to copy the CLAP bundle as well (per RESEARCH.md TBD #4 fallback), then re-run the build and re-verify.

       Then check codesign:
       ```bash
       codesign -dv ~/Library/Audio/Plug-Ins/VST3/Dukko.vst3 2>&1 | grep -i 'signature'
       codesign -dv ~/Library/Audio/Plug-Ins/CLAP/Dukko.clap 2>&1 | grep -i 'signature'
       ```
       Both should report a signature (ad-hoc is fine — the line typically reads `Signature=adhoc`). If either reports `code object is not signed at all`, add the post-build codesign command from RESEARCH.md Pitfall 4:
       ```cmake
       add_custom_command(TARGET Dukko POST_BUILD
           COMMAND codesign --sign - --force "$<TARGET_BUNDLE_DIR:Dukko>"
       )
       ```
       Re-build, re-verify.

    6. Run pluginval locally at strict-10 (smoke test before pushing — same command CI runs):
       ```bash
       curl -L -o /tmp/pluginval.zip https://github.com/Tracktion/pluginval/releases/download/v1.0.4/pluginval_macOS.zip
       unzip -o /tmp/pluginval.zip -d /tmp/
       chmod +x /tmp/pluginval.app/Contents/MacOS/pluginval
       /tmp/pluginval.app/Contents/MacOS/pluginval \
         --strictness-level 10 --validate-in-process \
         "build/Dukko_artefacts/Release/VST3/Dukko.vst3"
       ```
       Exit 0 = strict-10 passes — proceed to Task 2.

       **If strict-10 fails (per RESEARCH.md Pitfall 3 — most likely failure mode is state round-trip):** strict-10 is non-negotiable per QUAL-01 + Phase 1 success criterion #3. Fix in Phase 1, do NOT downgrade CI strictness. The minimum fix is to ensure the JUCE processor's `getStateInformation` / `setStateInformation` round-trips an empty `ValueTree` correctly:

       1. Open `source/PluginProcessor.cpp` (or whatever the renamed Pamplejuce processor file is — check `find source -name '*Processor.cpp'`).
       2. Locate `getStateInformation(MemoryBlock& destData)` and `setStateInformation(const void* data, int sizeInBytes)`.
       3. Replace the template's stub bodies with the minimum round-trippable implementation:
          ```cpp
          void DukkoAudioProcessor::getStateInformation (juce::MemoryBlock& destData)
          {
              juce::ValueTree state ("DukkoState");
              juce::MemoryOutputStream stream (destData, false);
              state.writeToStream (stream);
          }

          void DukkoAudioProcessor::setStateInformation (const void* data, int sizeInBytes)
          {
              juce::MemoryInputStream stream (data, static_cast<size_t> (sizeInBytes), false);
              auto state = juce::ValueTree::readFromStream (stream);
              juce::ignoreUnused (state); // Phase 2 will populate from chowdsp_plugin_state
          }
          ```
          Use the actual class name from the renamed source (likely `DukkoAudioProcessor` after Pamplejuce rename — verify by reading the header).
       4. Rebuild and re-run the pluginval command above.
       5. Document the source-level fix in `01-04-SUMMARY.md` under "Pitfall 3 resolution" so Phase 2 (chowdsp_plugin_state) knows to replace this stub with the proper versioned-state implementation.

       Repeat the rebuild + pluginval cycle until strict-10 exits 0. Do NOT proceed to Task 2 until strict-10 passes locally — pushing a known-failing build only burns CI minutes.
  </action>
  <verify>
    <automated>set -e; lipo -archs build/Dukko_artefacts/Release/VST3/Dukko.vst3/Contents/MacOS/Dukko | grep -w arm64 &amp;&amp; lipo -archs build/Dukko_artefacts/Release/CLAP/Dukko.clap/Contents/MacOS/Dukko | grep -w arm64 &amp;&amp; test -d ~/Library/Audio/Plug-Ins/VST3/Dukko.vst3 &amp;&amp; test -d ~/Library/Audio/Plug-Ins/CLAP/Dukko.clap &amp;&amp; codesign -dv ~/Library/Audio/Plug-Ins/VST3/Dukko.vst3 2&gt;&amp;1 | grep -qi signature &amp;&amp; codesign -dv ~/Library/Audio/Plug-Ins/CLAP/Dukko.clap 2&gt;&amp;1 | grep -qi signature</automated>
  </verify>
  <acceptance_criteria>
    - `cmake --build build --config Release` exits 0.
    - Both bundles exist at the conventional Pamplejuce paths (or the workflow YAML has been updated to match the actual paths).
    - `lipo -archs build/Dukko_artefacts/Release/VST3/Dukko.vst3/Contents/MacOS/Dukko | grep -w arm64` exits 0.
    - `lipo -archs build/Dukko_artefacts/Release/CLAP/Dukko.clap/Contents/MacOS/Dukko | grep -w arm64` exits 0.
    - Both bundles installed to `~/Library/Audio/Plug-Ins/`: `test -d ~/Library/Audio/Plug-Ins/VST3/Dukko.vst3 && test -d ~/Library/Audio/Plug-Ins/CLAP/Dukko.clap` exits 0.
    - Both bundles report a code signature (ad-hoc acceptable): `codesign -dv` output contains `Signature` for both bundles.
    - Local pluginval strict-10 exits 0. (Strict-10 is non-negotiable per QUAL-01 — if the unmodified Pamplejuce shell fails, the in-Phase-1 source fix in step 6 is applied to `source/PluginProcessor.cpp` until strict-10 passes; CI strictness is NEVER downgraded.)
    - Bundle identifier check: `plutil -p build/Dukko_artefacts/Release/VST3/Dukko.vst3/Contents/Info.plist | grep -i CFBundleIdentifier` outputs `com.dukkoaudio.dukko` (sanity check on D-04 making it through the build).
  </acceptance_criteria>
  <done>Local Release build produces both bundles in arm64, both auto-installed to ~/Library/Audio/Plug-Ins/, both ad-hoc-signed, pluginval strict-10 PASSES (strict-10 is non-negotiable; if it failed, the in-Phase-1 source-level fix in step 6 was applied). Workflow YAML matches the actual build paths. All execution-time TBDs (#4, #5, #6) resolved.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create GitHub repo "Dukko" (public) and provide its org/owner name</name>
  <what-built>
    Plans 01-03 created a Dukko-named tree, dependency-pinned, with a CI workflow ready to run on first push. Task 1 of this plan verified the local build is healthy (arm64 + ad-hoc-signed + bundles in ~/Library/Audio/Plug-Ins/).

    The README.md from plan 01 contains a placeholder badge URL `https://github.com/<your-org>/Dukko/actions/workflows/build.yml/badge.svg`. Before pushing, that placeholder needs the actual GitHub org/owner name, which only the user can supply (and the GitHub repo itself must be created so the push has somewhere to go).
  </what-built>
  <how-to-verify>
    User performs (one-time setup):

    1. Create the public GitHub repo `Dukko`. Two equivalent paths:
       - **Option A (gh CLI):** From the working tree root: `gh repo create Dukko --public --source . --remote origin --description "Tempo-locked, click-and-go ducking for Bitwig on Apple Silicon — VST3 + CLAP."` (this also sets the `origin` remote).
       - **Option B (web UI):** Visit https://github.com/new, name = `Dukko`, visibility = Public, do NOT initialise with a README/license/.gitignore (we have our own). Then locally: `git remote add origin git@github.com:<your-username>/Dukko.git`.

    2. Reply to this checkpoint with the GitHub owner/org name (e.g. `aottens` or `dukko-audio`) so task 3 can substitute it into the README badge URL.

    Per D-08 the repo MUST be public — free unlimited macos-14 Actions minutes depend on it.
  </how-to-verify>
  <resume-signal>Reply with the GitHub owner/org slug for the Dukko repo (e.g. "aottens"). Confirm the repo exists at https://github.com/&lt;owner&gt;/Dukko and that `git remote -v` shows origin pointing at it.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Substitute owner into README badge URL, commit, push, observe CI</name>
  <files>README.md</files>
  <read_first>
    - README.md (post-plan-01 — contains the literal `<your-org>` placeholder in the badge URL)
    - The user-supplied owner slug from task 2
    - .planning/phases/01-build-foundation-ci/01-VALIDATION.md §"Manual-Only Verifications" (the README-badge-renders-green check)
  </read_first>
  <action>
    1. Substitute the placeholder in README.md. Replace exactly two occurrences of `<your-org>` with the user-supplied slug. After substitution:
       ```bash
       grep '<your-org>' README.md
       ```
       must return empty.

    2. Stage everything from plans 01-04 and commit. Per plan 01's note (D-07 commit divergence), the first commit on the Dukko repo should clearly mark the divergence point. Suggested commit message:
       ```
       chore: ingest Pamplejuce template, rename to Dukko, wire CI

       - Pamplejuce → Dukko rename (D-01..D-05; permanent identifiers
         baked into CMakeLists.txt with do-not-change comment)
       - JUCE 8.0.12 + clap-juce-extensions @ <SHA-snippet>
         + chowdsp_utils v2.4.0 pinned via CPM (D-15, D-16)
       - LICENSES.md tracking all deps + CI tools (QUAL-05)
       - GitHub Actions workflow: pluginval strict-10 + clap-validator
         + Steinberg validator + Debug+ASan compile-only on macos-14
         (D-10, D-11, D-12, D-13, D-14)

       Closes Phase 1.
       ```
       Use `git commit` (no `--amend`, no `--no-verify`).

    3. Push:
       ```bash
       git push -u origin main
       ```
       (or `master` depending on repo default — `gh repo create` from task 2 follows the user's account default).

    4. Watch the CI run:
       ```bash
       gh run watch
       ```
       (or `gh run list` then `gh run view <id>`). Wait for the workflow to complete. Both jobs (`build-and-validate` and `build-debug-asan`) MUST report success.

       If the run fails:
       - **pluginval strict-10 failure:** see RESEARCH.md Pitfall 3 — this is the most likely failure mode for an unmodified Pamplejuce shell. Apply the in-Phase-1 source-level state-round-trip fix from Task 1 step 6 (modify `source/PluginProcessor.cpp`), rebuild, re-verify locally with strict-10, and re-push. Do NOT downgrade CI strictness — QUAL-01 requires strict-10.
       - **Steinberg validator path missing:** `find build/_deps -name validator` returned empty. Inspect logs, locate the correct path, update workflow.
       - **CLAP path mismatch:** the workflow YAML references a path that doesn't exist. Fix the workflow path and re-push.
       - **clap-validator asset filename mismatch:** the dynamic curl from the GitHub API may return no assets if the release schema changed. Hardcode the verified URL.
       - **Cache key parse error:** unlikely but possible if `hashFiles` glob is malformed. Fix and re-push.

       Resolve until the CI badge is GREEN. This is the BUILD-04 + QUAL-01 acceptance gate for Phase 1.

    5. Manual-only verification (Bitwig load — per VALIDATION.md):
       1. Open Bitwig (latest stable).
       2. Plugin browser → search "Dukko".
       3. Confirm BOTH variants appear under vendor "Dukko Audio": one VST3, one CLAP.
       4. Drag the VST3 onto an empty audio track. Confirm it instantiates without error.
       5. Drag the CLAP onto a separate empty audio track. Confirm it instantiates without error.
       6. Open Activity Monitor → find Bitwig process → confirm "Kind" column reads "Apple" (arm64), not "Intel" (Rosetta).
       7. README badge: open `https://github.com/<owner>/Dukko` in a browser. The "Build & Validate: passing" badge must render green at the top of the README.

       Record any anomalies. If steps 1-7 all pass, Phase 1 is COMPLETE.

    6. Update STATE.md: increment `completed_phases`, set Phase 1 status to "completed", add a Decisions row noting the actual JUCE / clap-juce-extensions / chowdsp_utils version pins (per CONTEXT.md "Integration Points" — STATE.md needs this entry at phase completion).
  </action>
  <verify>
    <automated>set -e; ! grep -q '&lt;your-org&gt;' README.md &amp;&amp; git log --oneline -1 | grep -qi 'dukko\|phase 1\|pamplejuce' &amp;&amp; git remote -v | grep -q 'Dukko' &amp;&amp; gh run list --limit 1 --json conclusion --jq '.[0].conclusion' | grep -q 'success'</automated>
  </verify>
  <acceptance_criteria>
    - `grep '<your-org>' README.md` returns empty (placeholder substituted).
    - `grep -E 'github.com/[a-zA-Z0-9_-]+/Dukko/actions' README.md` matches (badge URL points at a real org/repo).
    - The git remote `origin` exists and points at `github.com/<owner>/Dukko`: `git remote get-url origin | grep -q 'Dukko'` exits 0.
    - The first push succeeded: `git rev-list --count origin/main` (or master) >= 1.
    - The most recent Actions run for the Dukko repo on the pushed branch reports `conclusion = success`: `gh run list --limit 1 --json conclusion --jq '.[0].conclusion'` outputs `success`. (If this is run before the workflow has completed, wait until `gh run watch` returns.)
    - Manual-only checks (recorded in 01-04-SUMMARY.md, not automatable):
      - Bitwig plugin browser shows Dukko VST3 + CLAP under vendor "Dukko Audio". (Phase 1 SC1)
      - Both variants instantiate on an empty audio track without error. (BUILD-02)
      - Activity Monitor shows Bitwig running as Apple silicon (arm64), not Rosetta. (BUILD-02)
      - README badge renders green on the GitHub repo landing page. (BUILD-04 visual confirmation)
    - STATE.md updated with the actual three dep version pins recorded in the Decisions table and Phase 1 status set to completed.
  </acceptance_criteria>
  <done>The owner slug is in README.md, the first commit covers all Phase 1 work, the push has succeeded, the CI run is green, both variants load in Bitwig as arm64-native plugins, the README badge is green, and STATE.md reflects Phase 1 completion. Phase 1 is closed; Phase 2 can begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local working tree ↔ GitHub (first push) | The first push transfers the entire Phase 1 deliverable — all four plans' work — to GitHub. After this point, the repo is the canonical Dukko source of truth. |
| Local Bitwig process ↔ ad-hoc-signed Dukko bundle | Gatekeeper allows ad-hoc-signed bundles in `~/Library/Audio/Plug-Ins/`; Bitwig loads them without prompt. |
| GitHub Actions runner ↔ Dukko source code | First CI run executes Dukko's `cmake -B build && cmake --build build` plus the validator binaries against the resulting Dukko bundles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | First push to a public repo (D-08) exposes the entire codebase | accept | Public repo per D-08 (free unlimited macos-14 Actions minutes outweigh the secrecy cost; no commercial release decision yet). The repo contains no secrets — `.planning/` is product/research, `CLAUDE.md` is project canon, source/ is template scaffold from Pamplejuce. License headers and LICENSES.md document third-party rights cleanly. |
| T-04-02 | Tampering | Bitwig loads an ad-hoc-signed bundle from `~/Library/Audio/Plug-Ins/` | accept | The bundle is built locally from source the developer just authored. Ad-hoc signing is the macOS Gatekeeper-mandated minimum on Apple Silicon (RESEARCH.md Pitfall 4); a Developer ID is deferred to commercial release per CONTEXT.md "Deferred Ideas". No higher trust level is achievable in v1. |
| T-04-03 | Repudiation | Owner slug in README badge URL points to an org that may change ownership | accept | The badge URL is informational; if the repo moves, the URL is updated in a routine commit. No security-sensitive use of this URL. |
| T-04-04 | Elevation of Privilege | First Actions run executes against developer's GitHub token | accept | Standard `GITHUB_TOKEN` scope for a public-repo workflow. No secrets touched (Phase 1 uses no `secrets.*` references). Workflow permissions limited to default checkout/upload/cache. |
</threat_model>

<verification>
- All acceptance_criteria above pass.
- `gh run list --limit 1` reports `success` on the latest workflow run for the Dukko repo on the pushed branch.
- Manual Bitwig load test recorded in 01-04-SUMMARY.md as PASS or with documented issues.
- STATE.md reflects Phase 1 completion + the recorded dep pins.
</verification>

<success_criteria>
- BUILD-02 closure: `lipo -archs` reports `arm64` for both Dukko.vst3/Contents/MacOS/Dukko and Dukko.clap/Contents/MacOS/Dukko, AND Bitwig loads both variants successfully (manual verification).
- BUILD-04 closure (first-push portion): the macos-14 workflow ran on a real push and went green.
- QUAL-01 closure (first-push portion): pluginval strict-10 passed in CI. (Strict-10 is the only acceptable outcome — no fallback.)
- Phase 1 success criteria #1, #2, #3, #4 from ROADMAP.md all verifiably true: build produces arm64 VST3+CLAP, no Pamplejuce/KickstartClone strings, every-push CI green, LICENSES.md complete.
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-foundation-ci/01-04-SUMMARY.md` documenting:
- The recorded GitHub repo URL.
- The actual VST3 / CLAP build paths (resolved TBD #6).
- COPY_PLUGIN_AFTER_BUILD behaviour for CLAP (resolved TBD #4).
- Codesign behaviour (resolved TBD #5: did Pamplejuce auto-sign or did we add the post-build command?).
- pluginval strict-10 outcome on the unmodified Pamplejuce shell (passed-as-is / required-Pitfall-3-source-fix). If the source fix was applied, list the file/symbol modified and link to RESEARCH.md Pitfall 3 so Phase 2 knows to revisit when wiring chowdsp_plugin_state.
- First Actions run URL + conclusion.
- Bitwig manual load result (notes for both VST3 and CLAP).
- Confirmation that STATE.md was updated with phase completion + dep pins.

Then write `.planning/phases/01-build-foundation-ci/PHASE-SUMMARY.md` (if convention used by /gsd-verify-work) rolling up the four plan SUMMARYs into a single Phase 1 retrospective entry: what shipped, what was deferred, what TBDs were resolved, what to remember for Phase 2.
</output>
