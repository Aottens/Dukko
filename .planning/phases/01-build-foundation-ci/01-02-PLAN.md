---
phase: 01-build-foundation-ci
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - CMakeLists.txt
  - LICENSES.md
autonomous: true
requirements: [BUILD-03, QUAL-05]
must_haves:
  truths:
    - "CMakeLists.txt has CPM blocks pinning JUCE 8.0.12, clap-juce-extensions @ <SHA>, and chowdsp_utils v2.4.0 (per D-15, D-16)."
    - "CMakeLists.txt calls clap_juce_extensions_plugin(TARGET Dukko CLAP_ID \"com.dukkoaudio.dukko\" CLAP_FEATURES audio-effect mixing) AFTER juce_add_plugin (per BUILD-03)."
    - "LICENSES.md exists at repo root with the table format from D-17 listing every CPM-resolved dep + every CI-only validator tool, with the GPLv3-pluginval reasoning paragraph."
    - "The 'Use this template'-style ingestion (D-07) has not silently regressed — D-01..D-04 + the PERMANENT comment from plan 01 are still present after these edits."
  artifacts:
    - path: "CMakeLists.txt"
      provides: "Three CPM blocks (JUCE/clap-juce-extensions/chowdsp_utils) + clap_juce_extensions_plugin() call wiring CLAP onto the JUCE plugin target"
      contains: "clap_juce_extensions_plugin"
    - path: "LICENSES.md"
      provides: "Hand-curated dep + CI-tools table per D-17 format; GPLv3-pluginval reasoning paragraph documenting that the test-runner does not taint Dukko's license"
      contains: "Dukko Audio"
  key_links:
    - from: "CMakeLists.txt"
      to: "github.com/free-audio/clap-juce-extensions"
      via: "CPMAddPackage GIT_TAG <SHA>"
      pattern: "clap-juce-extensions"
    - from: "CMakeLists.txt"
      to: "Dukko target"
      via: "clap_juce_extensions_plugin(TARGET Dukko ...)"
      pattern: "clap_juce_extensions_plugin"
    - from: "LICENSES.md"
      to: "build/_deps/"
      via: "QUAL-05 enumeration check (every dep dir name appears in LICENSES.md)"
      pattern: "Dep | Version pin | License"
---

<objective>
Wire the three runtime dependencies (JUCE 8.0.12, clap-juce-extensions @ pinned SHA, chowdsp_utils v2.4.0) into CMakeLists.txt via CPM blocks and connect CLAP via `clap_juce_extensions_plugin()`. Create LICENSES.md at repo root in the D-17 format. After this plan, `cmake -B build && cmake --build build --config Release` should produce both `Dukko.vst3` AND `Dukko.clap` bundles locally.

Purpose: BUILD-03 (CLAP ships from Phase 1, not deferred — Bitwig is CLAP-native) and QUAL-05 (LICENSES.md tracks all deps from day 1, locking the format before more deps are added in later phases).

Output: Buildable plugin tree producing both formats locally. CI workflow editing is plan 03; first-push verification is plan 04.

Sequencing: depends on plan 01 because both touch CMakeLists.txt — plan 01 owns the rename + identifier permanence, plan 02 ADDS dependency blocks on top of that. The PERMANENT comment block from plan 01 must remain intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-build-foundation-ci/01-CONTEXT.md
@.planning/phases/01-build-foundation-ci/01-RESEARCH.md
@.planning/phases/01-build-foundation-ci/01-VALIDATION.md
@.planning/phases/01-build-foundation-ci/01-01-SUMMARY.md
@CLAUDE.md
@CMakeLists.txt

<interfaces>
<!-- Dep pin map (from RESEARCH.md "Dependency Pins" table). -->

JUCE                  → tag 8.0.12 (released 2024-12-16; latest stable 8.0.x)
clap-juce-extensions  → main HEAD SHA at clone time (no release tags exist) — TBD-during-execution #2
chowdsp_utils         → tag v2.4.0 (released 2025-11-29; BSD-3-Clause)

CPM block templates (from RESEARCH.md "CMake CPM Block Sketches"):

CPMAddPackage(
    NAME JUCE
    GIT_REPOSITORY https://github.com/juce-framework/JUCE.git
    GIT_TAG 8.0.12
    GIT_SHALLOW TRUE
)
# IMPORTANT: TBD-during-execution #3 — Pamplejuce may already pull JUCE via git
# submodule rather than CPM. If so, update the submodule ref to 8.0.12 instead of
# adding this CPM block. Inspect the rsynced tree first.

CPMAddPackage(
    NAME clap-juce-extensions
    GIT_REPOSITORY https://github.com/free-audio/clap-juce-extensions.git
    GIT_TAG <SHA-recorded-from-git-ls-remote>
    GIT_SHALLOW FALSE   # shallow clone fails for non-tag SHAs
)

CPMAddPackage(
    NAME chowdsp_utils
    GIT_REPOSITORY https://github.com/Chowdhury-DSP/chowdsp_utils.git
    GIT_TAG v2.4.0
    GIT_SHALLOW TRUE
)

# AFTER juce_add_plugin("Dukko" ...) — wires CLAP target onto the JUCE plugin:
clap_juce_extensions_plugin(
    TARGET Dukko
    CLAP_ID "com.dukkoaudio.dukko"          # D-04
    CLAP_FEATURES audio-effect mixing
)

LICENSES.md format template (from RESEARCH.md "LICENSES.md Format" — copy verbatim with the recorded SHA filled in).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CPM dep blocks + CLAP wiring to CMakeLists.txt</name>
  <files>CMakeLists.txt</files>
  <read_first>
    - CMakeLists.txt (current state, post-plan-01 — see how Pamplejuce currently fetches JUCE: CPM block, git submodule, or `add_subdirectory`)
    - cmake/CPM.cmake (confirm CPM is already vendored by Pamplejuce; per ASSUMED A1 in RESEARCH.md)
    - .planning/phases/01-build-foundation-ci/01-RESEARCH.md §"CMake CPM Block Sketches" (lines ~200–262)
    - .planning/phases/01-build-foundation-ci/01-RESEARCH.md §"Open TBDs" entries #2 and #3 (need to resolve at execution time)
    - .planning/phases/01-build-foundation-ci/01-CONTEXT.md (D-15: chowdsp in Phase 1; D-16: pinned tags/SHAs)
  </read_first>
  <action>
    1. **Resolve TBD #2 (clap-juce-extensions SHA).** Run:
       ```bash
       git ls-remote https://github.com/free-audio/clap-juce-extensions.git refs/heads/main
       ```
       Record the full 40-character commit SHA from the first column of output. This is the value that gets substituted for `<SHA-recorded-from-git-ls-remote>` in the CPM block below.

    2. **Resolve TBD #3 (JUCE fetch mechanism).** Inspect CMakeLists.txt for how Pamplejuce currently brings JUCE in:
       - If you find `CPMAddPackage(... NAME JUCE ...)` already → update its `GIT_TAG` to `8.0.12`.
       - If you find `add_subdirectory(JUCE)` and a `JUCE/` git submodule directory → run `git -C JUCE checkout 8.0.12` and re-stage the submodule pointer (also document this in the SUMMARY).
       - If neither pattern is present → add the CPM block from the `<interfaces>` section above.

       Whichever path applies, the end-state must be: JUCE pinned to tag `8.0.12`.

    3. **Add the clap-juce-extensions CPM block** with the recorded SHA, placed near the JUCE fetch (so all dep blocks live in one section of CMakeLists.txt). Use `GIT_SHALLOW FALSE` because shallow clone fails for non-tag SHAs.

    4. **Add the chowdsp_utils CPM block** at tag `v2.4.0`. Place it adjacent to the other CPM blocks.

    5. **After `juce_add_plugin(...)` returns**, add the `clap_juce_extensions_plugin(...)` call wiring `Dukko` as the target with `CLAP_ID "com.dukkoaudio.dukko"` (per D-04) and `CLAP_FEATURES audio-effect mixing`. This is the single line that makes the build produce a `.clap` bundle alongside the `.vst3`.

    6. **Do NOT modify the FORMATS line.** `set(FORMATS VST3)` from plan 01 stays as-is — clap-juce-extensions adds CLAP via its own macro, not via the JUCE FORMATS list. (Per RESEARCH.md "Note on FORMATS".)

    7. **Do NOT add chowdsp link targets yet.** Per RESEARCH.md, Phase 1 only needs the dep present so LICENSES.md is locked from day one (D-15 rationale). Phase 2 will add `target_link_libraries(Dukko PRIVATE chowdsp::chowdsp_plugin_state)` when versioned state actually gets implemented. A `# Phase 2 will link chowdsp::chowdsp_plugin_state` comment near the chowdsp CPM block makes this explicit.

    8. **Verify the PERMANENT comment block from plan 01 is still intact** (D-01..D-04 must still be flagged as PERMANENT). If the edits in this task displaced the comment, restore it.

    9. **Self-invalidating grep gate guard:** when verifying `clap_juce_extensions_plugin` is present, do NOT use `grep -c '^clap_juce_extensions_plugin'` against an unfiltered file (would count comments). Use `grep -n 'clap_juce_extensions_plugin(TARGET Dukko' CMakeLists.txt | grep -v '^[[:space:]]*#'` to filter comment-only matches.
  </action>
  <verify>
    <automated>set -e; grep -q 'GIT_TAG 8.0.12' CMakeLists.txt &amp;&amp; grep -q 'GIT_TAG v2.4.0' CMakeLists.txt &amp;&amp; grep -q 'clap-juce-extensions' CMakeLists.txt &amp;&amp; grep -nE '^[^#]*clap_juce_extensions_plugin\(' CMakeLists.txt &amp;&amp; grep -q 'CLAP_ID "com.dukkoaudio.dukko"' CMakeLists.txt &amp;&amp; grep -q 'PERMANENT — DO NOT CHANGE — see D-01..D-04' CMakeLists.txt &amp;&amp; cmake -B build -DCMAKE_BUILD_TYPE=Release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'GIT_TAG 8.0.12' CMakeLists.txt` exits 0 (JUCE pinned to 8.0.12, whether via CPM or submodule rev).
    - `grep -q 'GIT_TAG v2.4.0' CMakeLists.txt` exits 0 (chowdsp_utils pinned).
    - `grep -q 'clap-juce-extensions' CMakeLists.txt` exits 0 (clap-juce-extensions CPM block present).
    - `grep -nE '^[^#]*clap_juce_extensions_plugin\(' CMakeLists.txt` returns at least one match (call exists outside a comment).
    - `grep -q 'CLAP_ID "com.dukkoaudio.dukko"' CMakeLists.txt` exits 0 (D-04 wired into CLAP wrapper).
    - `grep -q 'PERMANENT — DO NOT CHANGE — see D-01..D-04' CMakeLists.txt` exits 0 (plan 01's permanence comment survived).
    - `cmake -B build -DCMAKE_BUILD_TYPE=Release` exits 0 (configure succeeds end-to-end with all three CPM blocks resolving).
    - Configure output mentions the three deps being downloaded/cached (visual confirmation in the build log; not a hard automated assertion since CPM cache state varies).
    - The recorded clap-juce-extensions SHA is preserved as a comment near the CPM block, e.g. `# clap-juce-extensions main @ <recorded date> — see TBD-during-execution #2`.
  </acceptance_criteria>
  <done>CMakeLists.txt has all three runtime deps pinned per D-15/D-16; `clap_juce_extensions_plugin()` wires CLAP onto the Dukko target with the correct ID; `cmake -B build` configures cleanly; PERMANENT comment block from plan 01 still in place.</done>
</task>

<task type="auto">
  <name>Task 2: Create LICENSES.md per D-17 format</name>
  <files>LICENSES.md</files>
  <read_first>
    - .planning/phases/01-build-foundation-ci/01-RESEARCH.md §"LICENSES.md Format" (lines ~415–448 — the verbatim template)
    - .planning/phases/01-build-foundation-ci/01-CONTEXT.md (D-17: format spec; QUAL-05: scope)
    - .planning/phases/01-01-SUMMARY.md (Pamplejuce fetch SHA recorded by plan 01 — informational, not a dep)
    - The recorded clap-juce-extensions SHA from task 1 (look in your CMakeLists.txt or the SUMMARY-in-progress)
  </read_first>
  <action>
    Create `/Users/aernoutottens/Documents/Projects/KickstartClone/LICENSES.md` at repo root (exactly that path — not under .planning/ or docs/).

    Contents (use the verbatim template from RESEARCH.md §"LICENSES.md Format" lines ~419–445), with these substitutions:
    - `<SHA>` for clap-juce-extensions → the actual 40-char SHA recorded in task 1
    - `Last updated:` → today's date in ISO format (`2026-05-03`)

    The file MUST contain two tables under two headings:

    **## Bundled / Linked Dependencies** — table with columns `Dep | Version pin | License | Source URL | License text`. Rows for: JUCE 8.0.12, clap-juce-extensions main @ `<SHA>`, chowdsp_utils v2.4.0, CPM.cmake (bundled), CLAP headers (transitive via clap-juce-extensions).

    **## CI-Only Tools (not linked into Dukko; license does not affect plugin)** — table with columns `Tool | Version | License | Note`. Rows for: pluginval 1.0.4 (GPLv3), clap-validator 0.3.2 (TBD verify license — leave the literal text "TBD-during-execution: verify license" in the License column for now per RESEARCH.md TBD #8), Steinberg VST3 validator (bundled in JUCE, Steinberg proprietary).

    **GPLv3 reasoning paragraph:** include verbatim from RESEARCH.md (the paragraph that begins "GPLv3 note on pluginval:"). This documents WHY pluginval's GPLv3 license does not taint Dukko — pluginval is a test-runner subprocess, not linked code, no derivative-work relationship.

    Header paragraph at the top of the file (before the first table):
    ```
    # Dukko — Third-Party Licenses

    This file tracks all third-party dependencies bundled with or linked into Dukko,
    plus CI tools that run against the plugin binary. Re-verify at every dep bump and
    at any commercial-release decision.

    Last updated: 2026-05-03
    ```

    **Self-invalidating grep gate guard:** when verifying dep names appear in LICENSES.md, the `grep -qi <name>` calls in the verify command target the table rows; comment lines and prose can also match (e.g. "Note: pluginval is GPLv3"). That is acceptable for QUAL-05 — the requirement is "tracks the dep", not "tracks it in a specific cell". Do not introduce a `grep -c` count gate here.
  </action>
  <verify>
    <automated>set -e; test -f LICENSES.md &amp;&amp; grep -q '^# Dukko — Third-Party Licenses' LICENSES.md &amp;&amp; grep -q 'Bundled / Linked Dependencies' LICENSES.md &amp;&amp; grep -q 'CI-Only Tools' LICENSES.md &amp;&amp; grep -qi '8.0.12' LICENSES.md &amp;&amp; grep -qi 'v2.4.0' LICENSES.md &amp;&amp; grep -qi 'clap-juce-extensions' LICENSES.md &amp;&amp; grep -qi 'JUCE' LICENSES.md &amp;&amp; grep -qi 'chowdsp_utils' LICENSES.md &amp;&amp; grep -qi 'pluginval' LICENSES.md &amp;&amp; grep -qi 'clap-validator' LICENSES.md &amp;&amp; grep -qi 'Steinberg' LICENSES.md &amp;&amp; grep -qi 'GPLv3' LICENSES.md &amp;&amp; grep -qi 'does not taint' LICENSES.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f LICENSES.md` exits 0 (file exists at repo root, not nested anywhere).
    - `head -1 LICENSES.md` outputs exactly `# Dukko — Third-Party Licenses`.
    - `grep -q 'Last updated: 2026-05-03' LICENSES.md` exits 0.
    - `grep -q '## Bundled / Linked Dependencies' LICENSES.md` exits 0.
    - `grep -q '## CI-Only Tools' LICENSES.md` exits 0.
    - All seven dep/tool names appear (one grep -qi each, all exit 0): `JUCE`, `clap-juce-extensions`, `chowdsp_utils`, `CPM.cmake`, `pluginval`, `clap-validator`, `Steinberg`.
    - All three version pins appear: `grep -qi '8.0.12'`, `grep -qi 'v2.4.0'`, `grep -qi '1.0.4'`, `grep -qi '0.3.2'` (all exit 0).
    - The clap-juce-extensions row contains the actual recorded SHA (not `<SHA>`): `grep -E 'clap-juce-extensions.*[a-f0-9]{40}' LICENSES.md` returns at least one match.
    - The GPLv3 reasoning paragraph is present: `grep -qi 'does not taint' LICENSES.md` exits 0.
    - QUAL-05 enumeration check (post-build, optional but recommended to dry-run): `for d in build/_deps/*/; do n=$(basename "$d" | sed 's/-src$//'); grep -qi "$n" LICENSES.md || echo "MISSING: $n"; done` outputs nothing. (May be deferred to plan 04 verification if `build/_deps/` is not yet populated at this task's verify time.)
  </acceptance_criteria>
  <done>LICENSES.md exists at repo root with both required tables, all seven deps/tools tracked, all version pins correct, GPLv3 reasoning paragraph present. QUAL-05 lock established for the rest of the project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local CMake configure ↔ GitHub (CPM fetches three deps over HTTPS) | Three external repos are pulled into `build/_deps/` during `cmake -B build`. Plan 04 runs the first build; plan 02 only sets up the configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Supply-chain — JUCE / clap-juce-extensions / chowdsp_utils CPM fetches | mitigate | D-16 mandates pinning to specific tags or SHAs (never `main`). Task 1 pins JUCE to `8.0.12` (tag), chowdsp to `v2.4.0` (tag), and clap-juce-extensions to a specific 40-char SHA captured from `git ls-remote` at execution time. Bumping any pin is a deliberate audited commit, not an implicit `git pull` of upstream HEAD. |
| T-02-02 | Information Disclosure | LICENSES.md GPLv3 reasoning paragraph | accept | The paragraph documents that pluginval's GPLv3 license does not taint Dukko (test-runner subprocess, not linked). This is intended public documentation — it is the legal record for any future commercial-release decision. No secret material exposed. |
| T-02-03 | Repudiation | LICENSES.md going stale (a future contributor adds a new CPM dep but forgets to update LICENSES.md) | mitigate | Plan 03 adds a CI step (`for d in build/_deps/*/; do grep -qi "$(basename ...)" LICENSES.md || exit 1; done`) that fails the workflow if any CPM-resolved dep is missing from LICENSES.md. The gate runs on every push — staleness becomes a blocking error before merge. |
</threat_model>

<verification>
- `cmake -B build -DCMAKE_BUILD_TYPE=Release` succeeds (configure-only, no build yet — plan 04 builds + verifies bundles).
- `LICENSES.md` exists at repo root with all required content.
- `git diff CMakeLists.txt` shows the three CPM blocks added + the `clap_juce_extensions_plugin()` call + comments noting the recorded SHA — and nothing else (PERMANENT block from plan 01 untouched).
</verification>

<success_criteria>
- BUILD-03 closure: `clap_juce_extensions_plugin(TARGET Dukko CLAP_ID "com.dukkoaudio.dukko" CLAP_FEATURES audio-effect mixing)` is in CMakeLists.txt and the configure step proves the CMake function is callable (clap-juce-extensions CPM resolved).
- QUAL-05 closure: LICENSES.md exists with the D-17 format, all current deps tracked, GPLv3 reasoning recorded.
- D-15 closure: chowdsp_utils v2.4.0 CPM block in place even though no module is linked yet (Phase 2 will link `chowdsp::chowdsp_plugin_state` without needing a CMake change).
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-foundation-ci/01-02-SUMMARY.md` documenting:
- The exact clap-juce-extensions SHA recorded (40 hex chars).
- Which of TBD #3's two paths applied (CPM-fetched JUCE vs git submodule vs neither — and what was done).
- The full LICENSES.md table snapshot at completion (so future bumps can diff against it).
- Output of `cmake -B build` (truncated to the dep-resolution lines) confirming all three deps fetched/cached.
</output>
