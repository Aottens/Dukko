---
phase: 01-build-foundation-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - CMakePresets.json
  - VERSION
  - README.md
  - .gitignore
  - source/PluginProcessor.h
  - source/PluginProcessor.cpp
  - source/PluginEditor.h
  - source/PluginEditor.cpp
  - .github/workflows/build.yml
  - packaging/
autonomous: true
requirements: [BUILD-01, BUILD-05]
must_haves:
  truths:
    - "Pamplejuce template has been ingested into the working folder via 'Use this template' (D-07) and an initial Dukko-rename commit exists."
    - "Repo root contains a CMakeLists.txt whose PROJECT_NAME='Dukko', PRODUCT_NAME='Dukko', COMPANY_NAME='Dukko Audio', BUNDLE_ID='com.dukkoaudio.dukko', PLUGIN_MANUFACTURER_CODE='Dukk', PLUGIN_CODE='Dkk1', FORMATS='VST3' (CLAP added in plan 02)."
    - "VERSION file at repo root reads exactly '0.1.0'."
    - "git grep -i 'kickstartclone\\|pamplejuce' against tracked files (excluding .planning/, CLAUDE.md, LICENSES.md) returns empty."
    - "A PERMANENT-DO-NOT-CHANGE comment block in CMakeLists.txt anchors D-01..D-04 against future casual edits."
  artifacts:
    - path: "CMakeLists.txt"
      provides: "Dukko CMake project with locked identifiers (D-01..D-05) and CMAKE_OSX_ARCHITECTURES=arm64, CMAKE_OSX_DEPLOYMENT_TARGET=11.0"
      contains: "Dukko Audio"
    - path: "VERSION"
      provides: "Semver string consumed by Pamplejuce/JUCE plugin manifest"
      contains: "0.1.0"
    - path: "README.md"
      provides: "Dukko project landing page (no Pamplejuce template prose)"
      contains: "Dukko"
    - path: ".gitignore"
      provides: "Build/IDE artefact exclusions (build/, Builds/, .DS_Store, *.xcodeproj/, _deps/, cmake-build-*/, .idea/)"
  key_links:
    - from: "CMakeLists.txt"
      to: "VERSION"
      via: "Pamplejuce VERSION file → JUCE plugin manifest version macro"
      pattern: "VERSION"
    - from: "CMakeLists.txt"
      to: "source/"
      via: "juce_add_plugin SOURCES path"
      pattern: "source/"
---

<objective>
Ingest the Pamplejuce template into this working tree and rename every Pamplejuce/template string to Dukko, with the four PERMANENT identifiers (D-01..D-04) baked into CMakeLists.txt with a do-not-change comment. Pure rename + scaffold pass; no dependency wiring (plan 02), no CI workflow editing beyond identifier renames in job names (plan 03).

Purpose: BUILD-01 + BUILD-05. Establishes the Dukko name across CMake target, plugin manifest, binary names, and source/ template strings before any other plan touches the tree. D-01..D-04 are baked into binaries forever and cannot be safely changed once Bitwig has loaded the plugin even once.

Output: A locally-buildable Pamplejuce-derived tree where every string reads "Dukko" / "Dukko Audio" and the locked identifiers are in place. This plan does NOT yet add CLAP, chowdsp_utils, or wire CI validators — that is plans 02 and 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-build-foundation-ci/01-CONTEXT.md
@.planning/phases/01-build-foundation-ci/01-RESEARCH.md
@.planning/phases/01-build-foundation-ci/01-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Locked identifier values from CONTEXT.md (D-01..D-04). -->
<!-- These four values are PERMANENT — once a Bitwig project saves a reference to -->
<!-- com.dukkoaudio.dukko / Dukk / Dkk1, changing any of them in the future will -->
<!-- make every existing project report "plugin missing". -->

D-01 Manufacturer name      = "Dukko Audio"
D-02 Manufacturer code (4)  = "Dukk"
D-03 Plugin code (4)        = "Dkk1"
D-04 Bundle ID / CLAP ID    = "com.dukkoaudio.dukko"
D-05 Folder + repo name     = "Dukko" (capital D)
D-06 Source layout          = source/ (lowercase) — Pamplejuce default
D-09 Auto-install           = COPY_PLUGIN_AFTER_BUILD TRUE (Pamplejuce default; verify it stays TRUE)

Pamplejuce → Dukko CMakeLists.txt rename map (per RESEARCH.md "Exhaustive Rename Touchpoints"):
| Variable                            | Pamplejuce default               | Dukko value                 |
| set(PROJECT_NAME ...)               | "Pamplejuce"                     | "Dukko"                     |
| set(PRODUCT_NAME ...)               | "Pamplejuce Demo"                | "Dukko"                     |
| set(COMPANY_NAME ...)               | "Pamplejuce Company"             | "Dukko Audio"               |
| set(BUNDLE_ID ...)                  | "com.pamplejuce.pamplejuce"      | "com.dukkoaudio.dukko"      |
| set(FORMATS ...)                    | Standalone AU VST3 AUv3          | VST3   (CLAP added plan 02) |
| PLUGIN_MANUFACTURER_CODE            | "Pamp"                           | "Dukk"                      |
| PLUGIN_CODE                         | "P001"                           | "Dkk1"                      |
| project(Dukko VERSION ...)          | (template default)               | 0.1.0                       |
| CMAKE_OSX_ARCHITECTURES             | (template may set universal)     | "arm64"                     |
| CMAKE_OSX_DEPLOYMENT_TARGET         | (template may set higher)        | "11.0"                      |
| CMAKE_CXX_STANDARD                  | 23 (Pamplejuce default)          | 23  (LEAVE — Apple Clang 15+ supports it; CLAUDE.md says C++20 minimum, C++23 ⊇ C++20) |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ingest Pamplejuce template into working tree</name>
  <files>CMakeLists.txt, CMakePresets.json, VERSION, README.md, .gitignore, source/, packaging/, cmake/, .github/, assets/, .clang-format, .clang-tidy</files>
  <read_first>
    - .planning/phases/01-build-foundation-ci/01-RESEARCH.md §"Pamplejuce Ingestion & Rename Plan" (lines ~99–143)
    - .planning/phases/01-build-foundation-ci/01-CONTEXT.md §"Implementation Decisions" (D-05..D-09)
    - https://github.com/sudara/pamplejuce (current main HEAD — what files ship in the template)
    - .gitignore (current — there should be no real `.gitignore` yet beyond planning artefacts)
  </read_first>
  <action>
    Pamplejuce ingestion (D-07: "Use this template" then commit divergence). The user's preferred approach is the GitHub "Use this template" flow, but the working tree already exists at /Users/aernoutottens/Documents/Projects/KickstartClone with .planning/ + CLAUDE.md committed. To honor D-05 (rename to Dukko) without disrupting the .planning/ history, do this:

    1. **Pull Pamplejuce as a tarball** (avoids mixing template git history into Dukko's own log). From the working tree root:
       ```bash
       cd /tmp
       curl -L https://github.com/sudara/pamplejuce/archive/refs/heads/main.tar.gz -o pamplejuce.tar.gz
       tar xf pamplejuce.tar.gz
       ```
    2. **Copy template files into the working tree**, EXCLUDING `.git`, `.planning`, `CLAUDE.md`, `README.md` (we will write our own README in task 3):
       ```bash
       rsync -av \
         --exclude='.git' \
         --exclude='.planning' \
         --exclude='CLAUDE.md' \
         --exclude='README.md' \
         /tmp/pamplejuce-main/ /Users/aernoutottens/Documents/Projects/KickstartClone/
       ```
    3. **Confirm the expected Pamplejuce files now exist** in the working tree: `CMakeLists.txt`, `CMakePresets.json` (if present in template), `cmake/` directory (CPM.cmake lives here), `source/` directory with `PluginProcessor.{h,cpp}` + `PluginEditor.{h,cpp}`, `packaging/` directory, `.github/workflows/build.yml`, `.clang-format`, `.clang-tidy`, `.gitignore`, `VERSION` file.
    4. **Run** `git status` and confirm a large set of new untracked files appears. Do NOT stage or commit yet — task 2 does the rename pass first.

    Do NOT use `git submodule add` for Pamplejuce — D-07 is "commit divergence", which means we own these files outright from this commit forward. There is no upstream sync.

    Do NOT delete or modify `.planning/`, `CLAUDE.md`, or any file already in the repo. The rsync `--exclude` flags above protect them; verify with `git status` that no tracked file shows as modified.

    **PERMANENT — DO NOT CHANGE — see D-01..D-04 in CONTEXT.md** (this comment will be added to CMakeLists.txt in task 2; flagged here so you remember the constraint exists when you read the freshly-copied CMakeLists.txt).
  </action>
  <verify>
    <automated>test -f CMakeLists.txt &amp;&amp; test -f VERSION &amp;&amp; test -d source &amp;&amp; test -d cmake &amp;&amp; test -f .github/workflows/build.yml &amp;&amp; test -f .planning/STATE.md &amp;&amp; test -f CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f CMakeLists.txt` exits 0 (Pamplejuce CMake config present at repo root).
    - `test -f VERSION` exits 0 (Pamplejuce ships a VERSION file).
    - `test -d source` exits 0 and `test -f source/PluginProcessor.cpp` exits 0.
    - `test -d cmake` exits 0 (CPM.cmake lives in this directory per Pamplejuce convention).
    - `test -f .github/workflows/build.yml` exits 0.
    - `test -f .planning/STATE.md && test -f CLAUDE.md` exits 0 (pre-existing files were not touched by rsync).
    - `git diff --name-only HEAD -- .planning CLAUDE.md` returns empty (no modifications to pre-existing tracked files).
  </acceptance_criteria>
  <done>Pamplejuce template files exist in the working tree alongside the pre-existing .planning/ + CLAUDE.md, untracked, ready for rename pass in task 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rename CMakeLists.txt + source identifiers + bake D-01..D-04 with permanence comment</name>
  <files>CMakeLists.txt, CMakePresets.json, VERSION, source/PluginProcessor.h, source/PluginProcessor.cpp, source/PluginEditor.h, source/PluginEditor.cpp, packaging/, .github/workflows/build.yml</files>
  <read_first>
    - CMakeLists.txt (the freshly-rsynced Pamplejuce default, to see actual variable names)
    - CMakePresets.json (if present)
    - source/PluginProcessor.h, source/PluginProcessor.cpp, source/PluginEditor.h, source/PluginEditor.cpp (find any hardcoded "Pamplejuce" / "Demo" strings)
    - packaging/ (any .plist fragments with old bundle ID)
    - .github/workflows/build.yml (job names that say "Pamplejuce")
    - .planning/phases/01-build-foundation-ci/01-RESEARCH.md §"Exhaustive Rename Touchpoints" (the table of variables and their Dukko values)
    - .planning/phases/01-build-foundation-ci/01-CONTEXT.md §"Plugin Identifiers" (D-01..D-04)
  </read_first>
  <action>
    Apply the rename map from the `<interfaces>` block above to every Pamplejuce/template string. Concrete edits:

    **CMakeLists.txt at repo root:**
    1. Find `set(PROJECT_NAME ...)` → set value to `"Dukko"`. (per D-05)
    2. Find `set(PRODUCT_NAME ...)` → `"Dukko"`. (per D-05)
    3. Find `set(COMPANY_NAME ...)` → `"Dukko Audio"`. (per D-01)
    4. Find `set(BUNDLE_ID ...)` → `"com.dukkoaudio.dukko"`. (per D-04)
    5. Find `set(FORMATS ...)` → `VST3` ONLY for now. Remove `Standalone`, `AU`, `AUv3`. CLAP gets added in plan 02 via clap-juce-extensions, NOT via the FORMATS list.
    6. Find `juce_add_plugin(...)` block. Inside it:
       - `PLUGIN_MANUFACTURER_CODE Dukk` (per D-02)
       - `PLUGIN_CODE Dkk1` (per D-03)
       - Confirm `COPY_PLUGIN_AFTER_BUILD TRUE` is present (per D-09); add it if missing.
    7. Find `project(... VERSION ...)` line near the top → ensure it reads `project(Dukko VERSION 0.1.0)`.
    8. Add (or set) `set(CMAKE_OSX_ARCHITECTURES "arm64")` and `set(CMAKE_OSX_DEPLOYMENT_TARGET "11.0")` near the top of the file (after `cmake_minimum_required` but before `project()`).
    9. Leave `set(CMAKE_CXX_STANDARD 23)` alone (Pamplejuce default; C++23 ⊇ C++20).

    **PERMANENT comment block.** Immediately above the four locked identifier sets (PROJECT_NAME / COMPANY_NAME / BUNDLE_ID / juce_add_plugin block), insert this comment block VERBATIM:
    ```cmake
    # ==============================================================================
    # PERMANENT — DO NOT CHANGE — see D-01..D-04 in .planning/phases/01-build-foundation-ci/01-CONTEXT.md
    #
    # These four values are baked into the plugin binary and the VST3/CLAP manifests.
    # Once any Bitwig (or other host) project saves a reference to these IDs, changing
    # them will make every project that ever loaded Dukko report "plugin missing":
    #   - PROJECT_NAME / PRODUCT_NAME    = "Dukko"                    (D-05)
    #   - COMPANY_NAME (manufacturer)    = "Dukko Audio"              (D-01)
    #   - PLUGIN_MANUFACTURER_CODE       = "Dukk"                     (D-02)
    #   - PLUGIN_CODE                    = "Dkk1"                     (D-03)
    #   - BUNDLE_ID / CLAP_ID            = "com.dukkoaudio.dukko"     (D-04)
    # ==============================================================================
    ```

    **CMakePresets.json (if present):** rename any preset name strings containing "Pamplejuce" to use the "Dukko-" prefix instead.

    **VERSION file at repo root:** overwrite contents with exactly `0.1.0` (no trailing newline matter; Pamplejuce reads as-is).

    **source/ files:** `git grep -l -i 'pamplejuce\|demo' source/` will find every hit. For each, replace user-facing strings (window titles, GUI labels, log lines, plugin display name strings) with "Dukko". Leave structural code (class names like `PluginProcessor` or `PluginEditor`) UNCHANGED — those are JUCE-conventional class names, not template branding. Only branding strings get renamed.

    **packaging/ directory:** check every file for `com.pamplejuce.pamplejuce` → replace with `com.dukkoaudio.dukko`. Check for any `Pamplejuce` brand strings in .plist fragments → replace with `Dukko`.

    **.github/workflows/*.yml:** rename job names, artifact names, and any string containing "Pamplejuce" to "Dukko". Do NOT touch validator wiring (that is plan 03's scope) — only rename identifier strings.

    Final sanity sweep: run `git grep -i 'pamplejuce' -- ':!.planning/' ':!CLAUDE.md' ':!LICENSES.md'` — output MUST be empty. If any hit remains, fix it.

    **Self-invalidating grep gate guard:** when filtering excludes, do NOT use `grep -c` against unfiltered files (that counts comment lines too). Use the literal pathspec exclusions shown above.
  </action>
  <verify>
    <automated>set -e; test "$(cat VERSION | tr -d '[:space:]')" = "0.1.0" &amp;&amp; grep -q 'project(Dukko VERSION 0.1.0)' CMakeLists.txt &amp;&amp; grep -q 'set(PROJECT_NAME "Dukko")' CMakeLists.txt &amp;&amp; grep -q 'set(COMPANY_NAME "Dukko Audio")' CMakeLists.txt &amp;&amp; grep -q 'set(BUNDLE_ID "com.dukkoaudio.dukko")' CMakeLists.txt &amp;&amp; grep -q 'PLUGIN_MANUFACTURER_CODE Dukk' CMakeLists.txt &amp;&amp; grep -q 'PLUGIN_CODE Dkk1' CMakeLists.txt &amp;&amp; grep -q 'CMAKE_OSX_ARCHITECTURES.*arm64' CMakeLists.txt &amp;&amp; grep -q 'CMAKE_OSX_DEPLOYMENT_TARGET.*11.0' CMakeLists.txt &amp;&amp; grep -q 'PERMANENT — DO NOT CHANGE — see D-01..D-04' CMakeLists.txt &amp;&amp; ! git grep -i 'pamplejuce' -- ':!.planning/' ':!CLAUDE.md' ':!LICENSES.md' &amp;&amp; ! git grep -i 'kickstartclone' -- ':!.planning/' ':!CLAUDE.md' ':!LICENSES.md'</automated>
  </verify>
  <acceptance_criteria>
    - `cat VERSION` outputs exactly `0.1.0` (whitespace-trimmed).
    - `grep -q 'project(Dukko VERSION 0.1.0)' CMakeLists.txt` exits 0.
    - `grep -q 'set(PROJECT_NAME "Dukko")' CMakeLists.txt` exits 0.
    - `grep -q 'set(PRODUCT_NAME "Dukko")' CMakeLists.txt` exits 0.
    - `grep -q 'set(COMPANY_NAME "Dukko Audio")' CMakeLists.txt` exits 0.
    - `grep -q 'set(BUNDLE_ID "com.dukkoaudio.dukko")' CMakeLists.txt` exits 0.
    - `grep -q 'PLUGIN_MANUFACTURER_CODE Dukk' CMakeLists.txt` exits 0.
    - `grep -q 'PLUGIN_CODE Dkk1' CMakeLists.txt` exits 0.
    - `grep -q 'COPY_PLUGIN_AFTER_BUILD TRUE' CMakeLists.txt` exits 0 (D-09 preserved).
    - `grep -q 'CMAKE_OSX_ARCHITECTURES.*arm64' CMakeLists.txt` exits 0.
    - `grep -q 'CMAKE_OSX_DEPLOYMENT_TARGET.*11.0' CMakeLists.txt` exits 0.
    - `grep -q 'PERMANENT — DO NOT CHANGE — see D-01..D-04' CMakeLists.txt` exits 0 (permanence comment is in place).
    - `grep -E 'set\(FORMATS' CMakeLists.txt | grep -v 'CLAP'` matches and contains `VST3` (CLAP gets added by plan 02; FORMATS line should currently list only VST3).
    - `git grep -i 'pamplejuce' -- ':!.planning/' ':!CLAUDE.md' ':!LICENSES.md'` returns empty.
    - `git grep -i 'kickstartclone' -- ':!.planning/' ':!CLAUDE.md' ':!LICENSES.md'` returns empty.
  </acceptance_criteria>
  <done>Every Pamplejuce/KickstartClone string outside .planning/ + CLAUDE.md + LICENSES.md is gone; D-01..D-04 are baked into CMakeLists.txt with the permanence comment block; VERSION says 0.1.0; arm64 + 11.0 deployment target are set.</done>
</task>

<task type="auto">
  <name>Task 3: Write Dukko README + .gitignore audit</name>
  <files>README.md, .gitignore</files>
  <read_first>
    - .gitignore (the rsynced Pamplejuce default — confirm what is and isn't covered)
    - .planning/PROJECT.md (Dukko product description for the README)
    - CLAUDE.md §"Project" (one-paragraph product summary; match the voice — Dutch phrases for emphasis are fine)
    - .planning/phases/01-build-foundation-ci/01-RESEARCH.md §".gitignore baseline" (what entries should exist)
  </read_first>
  <action>
    **README.md:** overwrite with a Dukko-specific landing page. Include:
    1. Title: `# Dukko`
    2. One-line tagline: `Tempo-locked, click-and-go ducking for Bitwig on Apple Silicon — VST3 + CLAP.`
    3. CI status badge placeholder (will be wired by plan 03 / plan 04 once the GitHub repo URL is known):
       ```markdown
       [![Build & Validate](https://github.com/<your-org>/Dukko/actions/workflows/build.yml/badge.svg)](https://github.com/<your-org>/Dukko/actions/workflows/build.yml)
       ```
       Leave the literal `<your-org>` placeholder — plan 04 will substitute it after the GitHub repo is created.
    4. A short "What this is" paragraph (3–5 lines) summarising the project from CLAUDE.md §"Project". Match the existing voice (terse, occasional Dutch like "strak en bruikbaar" is fine).
    5. A short "Build" section showing the local build incantation:
       ```
       cmake -B build -DCMAKE_BUILD_TYPE=Release
       cmake --build build --config Release
       ```
    6. A "Status" section noting v1 is in active development.
    7. Do NOT include any Pamplejuce template prose, contributor instructions, or "this is a template" language.

    **.gitignore audit:** read the freshly-rsynced .gitignore. Confirm it covers (add any missing):
    - `build/`
    - `Builds/`
    - `.DS_Store`
    - `*.xcodeproj/`
    - `_deps/`
    - `cmake-build-*/`
    - `.idea/`
    - `.vscode/` (optional but common)
    - `pluginval-results/` (CI step writes here per RESEARCH.md workflow sketch)

    Append any missing entries at the bottom of .gitignore under a `# Dukko additions` comment header. Do NOT remove any existing Pamplejuce defaults — they are sensible.

    Verify `git grep -i 'pamplejuce\|kickstartclone' -- ':!.planning/' ':!CLAUDE.md' ':!LICENSES.md'` still returns empty after these edits.
  </action>
  <verify>
    <automated>set -e; test -f README.md &amp;&amp; head -1 README.md | grep -q '^# Dukko' &amp;&amp; ! grep -i pamplejuce README.md &amp;&amp; for entry in 'build/' 'Builds/' '.DS_Store' '\\*.xcodeproj/' '_deps/' 'cmake-build-' '.idea/'; do grep -q "$entry" .gitignore || { echo "MISSING in .gitignore: $entry"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 README.md` outputs exactly `# Dukko`.
    - `grep -q 'Tempo-locked' README.md` exits 0 (tagline present).
    - `grep -q 'cmake -B build' README.md` exits 0 (build instructions present).
    - `grep -i pamplejuce README.md` returns empty (no template prose leaked through).
    - `.gitignore` contains the entries `build/`, `Builds/`, `.DS_Store`, `*.xcodeproj/`, `_deps/`, `cmake-build-`, `.idea/` (one grep per entry, all exit 0).
    - `git grep -i 'pamplejuce\|kickstartclone' -- ':!.planning/' ':!CLAUDE.md' ':!LICENSES.md'` returns empty.
  </acceptance_criteria>
  <done>README.md is Dukko's own landing page (badge URL has a placeholder for plan 04); .gitignore covers all expected build/IDE artefacts; no template strings leaked into either file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem ↔ Pamplejuce tarball download | A network-fetched archive is unpacked into the working tree. No network ingress otherwise — this is the only network operation in plan 01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Pamplejuce tarball download in task 1 | accept | Pamplejuce is fetched once over HTTPS from github.com; integrity is whatever GitHub serves. Task 1 immediately commits the divergence point (D-07), so any tampering becomes part of the plugin's own audited history from then on. No supply-chain pinning at this stage — plan 02 pins the actual runtime deps (JUCE, clap-juce-extensions, chowdsp_utils) to specific tags/SHAs per D-16. |
| T-01-02 | Tampering / Repudiation | Permanent identifier comment in CMakeLists.txt | mitigate | Task 2 adds an explicit `# PERMANENT — DO NOT CHANGE — see D-01..D-04` comment block above the four locked identifier `set()` lines. Any future contributor (or future-Claude) sees the comment before editing and is warned that changing these breaks all existing Bitwig projects. |
| T-01-03 | Information Disclosure | README.md badge URL contains GitHub org name | accept | The repo is public per D-08; the org name is not a secret. The literal `<your-org>` placeholder is left for plan 04 to substitute after the GitHub repo is created. |
</threat_model>

<verification>
After all three tasks:
- `cd /Users/aernoutottens/Documents/Projects/KickstartClone && cat VERSION` → `0.1.0`
- `head -20 CMakeLists.txt` → contains `project(Dukko VERSION 0.1.0)` and the PERMANENT comment block.
- `git grep -i 'pamplejuce\|kickstartclone' -- ':!.planning/' ':!CLAUDE.md' ':!LICENSES.md'` → empty.
- `git status` → many new untracked files from Pamplejuce; no modifications to .planning/ or CLAUDE.md.
- The tree is NOT yet buildable end-to-end (CPM blocks for JUCE/CLAP/chowdsp come in plan 02). That is expected — plan 01 is rename-only.
</verification>

<success_criteria>
- BUILD-05 closure (in-scope portion): CMakeLists.txt + source/ + packaging/ + .github/ are all renamed; D-01..D-04 baked in with permanence comment; no Pamplejuce/KickstartClone strings remain outside the three excluded paths.
- BUILD-01 closure (foundation portion): Pamplejuce template ingested; project structure (CMakeLists.txt, source/, cmake/, .github/) exists in the working tree at correct paths.
- VERSION file at `0.1.0`; arm64 + macOS 11.0 deployment target set in CMake.
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-foundation-ci/01-01-SUMMARY.md` documenting:
- The Pamplejuce fetch SHA / commit (run `git -C /tmp/pamplejuce-main rev-parse HEAD` and record).
- All files renamed and rationale.
- Confirmation that D-01..D-04 are baked in with permanence comment.
- Any deviations from the rename map (there should be none).
</output>
